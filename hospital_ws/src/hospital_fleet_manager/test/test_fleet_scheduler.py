import pytest&#10;from unittest.mock import MagicMock, patch&#10;&#10;from hospital_fleet_manager.fleet_scheduler import FleetScheduler&#10;&#10;&#10;@pytest.fixture&#10;def scheduler():&#10;    with patch(&#39;hospital_fleet_manager.fleet_scheduler.get_package_share_directory&#39;, return_value=&#39;/tmp&#39;), &#10;         patch(&#39;hospital_fleet_manager.fleet_scheduler.yaml.safe_load&#39;, side_effect=[&#10;             {&#39;robots&#39;: {&#39;r1&#39;: &#39;A&#39;}},&#10;             {&#39;tasks&#39;: [&#39;t1&#39;]},&#10;         ]):&#10;        s = FleetScheduler()&#10;        s.predictor = MagicMock(predict=lambda d,c: 1.0)&#10;        s.hospital_map = MagicMock()&#10;        s.hospital_map.shortest_path_length = MagicMock(return_value=1)&#10;        return s&#10;&#10;&#10;def test_assign_tasks(scheduler):&#10;    assignments = scheduler.assign_tasks()&#10;    assert len(assignments) == 1  # 1 robot 1 task
